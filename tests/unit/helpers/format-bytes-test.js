import { formatBytes } from 'dummy/helpers/format-bytes';
import { module, test } from 'qunit';

module('Unit | Helper | format bytes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatBytes([42]);
  assert.ok(result);
});
